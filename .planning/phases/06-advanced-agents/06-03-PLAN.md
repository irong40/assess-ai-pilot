---
phase: 06-advanced-agents
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - supabase/functions/agent-pen-test/index.ts
  - supabase/functions/_shared/pen-test-schemas.ts
  - supabase/functions/_shared/pen-test-tools.ts
  - supabase/functions/_shared/ciso-tools.ts
  - supabase/migrations/20260327400002_pen_test_agent_tables.sql
  - src/lib/pen-test-schemas-frontend.ts
  - src/lib/pen-test-tools-testable.ts
  - src/lib/ciso-tools.ts
  - src/types/pen-test-output.ts
  - src/lib/__tests__/pen-test-schemas.test.ts
  - src/lib/__tests__/pen-test-agent.test.ts
  - src/lib/__tests__/pen-test-migration.test.ts
  - src/lib/__tests__/ciso-agent.test.ts
autonomous: true
requirements: [PENT-01, PENT-02, PENT-03, PENT-04]

must_haves:
  truths:
    - "Pen Test agent performs PASSIVE ONLY vulnerability discovery -- no HTTP requests, no DNS lookups, no port scans, no network probing"
    - "Pen Test agent matches the company's declared tech stack against known CVE patterns in threat_intelligence table"
    - "Pen Test agent generates vulnerability reports with risk ratings (critical/high/medium/low), exploitability assessment, and business impact"
    - "Pen Test agent requires explicit authorization (agent_permissions check) AND approval request (risk_level='high') before any scan"
    - "CISO Orchestrator can delegate to Pen Test agent via delegateToPenTest tool"
    - "All 7 agents are now delegatable from the CISO Orchestrator (GRC, SOC, ThreatIntel, IR, AppSec, PenTest)"
  artifacts:
    - path: "supabase/functions/agent-pen-test/index.ts"
      provides: "Pen Test Edge Function handler with 3 actions"
      contains: "executeAgentTask"
    - path: "supabase/functions/_shared/pen-test-schemas.ts"
      provides: "Zod schemas for Pen Test structured output"
      exports: ["PenTestFindingSchema", "VulnerabilityReportSchema"]
    - path: "supabase/functions/_shared/pen-test-tools.ts"
      provides: "Pen Test system prompt and 4 domain tools"
      exports: ["PEN_TEST_SYSTEM_PROMPT", "createPenTestTools"]
    - path: "supabase/migrations/20260327400002_pen_test_agent_tables.sql"
      provides: "pen_test_findings table with RLS"
      contains: "CREATE TABLE pen_test_findings"
    - path: "src/lib/__tests__/pen-test-schemas.test.ts"
      provides: "Schema validation tests for Pen Test output"
      min_lines: 30
    - path: "src/lib/__tests__/pen-test-agent.test.ts"
      provides: "Agent handler, authorization gate, and passive-only constraint tests"
      min_lines: 30
  key_links:
    - from: "supabase/functions/_shared/ciso-tools.ts"
      to: "supabase/functions/agent-pen-test/index.ts"
      via: "delegateToPenTest tool delegates tasks to pen-test agent type"
      pattern: "delegateToPenTest"
    - from: "supabase/functions/_shared/pen-test-tools.ts"
      to: "agent_permissions"
      via: "checkScanAuthorization verifies pen-test agent is enabled for company"
      pattern: "agent_permissions"
    - from: "supabase/functions/_shared/pen-test-tools.ts"
      to: "threat_intelligence"
      via: "matchTechStackCVEs cross-references tech stack against known CVEs"
      pattern: "threat_intelligence"
    - from: "src/lib/pen-test-tools-testable.ts"
      to: "src/lib/__tests__/pen-test-agent.test.ts"
      via: "Node mirror tested by vitest"
      pattern: "PEN_TEST_SYSTEM_PROMPT"
---

<objective>
Pen Test agent with passive-only vulnerability discovery, tech stack CVE matching, authorization gates, and vulnerability reports -- completing the full 7-agent security team.

Purpose: The Pen Test agent performs PASSIVE ONLY vulnerability discovery by matching the company's declared tech stack against known CVE patterns in the threat_intelligence table. No active exploitation, no network scanning, no port probing -- all analysis is against declared data in the database. Requires explicit authorization (agent_permissions check) AND risk_level='high' approval before any scan. This is the final agent, completing the "replace your security team" value proposition.

Output: Pen Test Edge Function, Zod schemas, 4 domain tools (including authorization gate), pen_test_findings migration, CISO delegateToPenTest extension (completing all 6 specialist delegations), vitest-compatible Node mirrors, test suite.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-advanced-agents/06-RESEARCH.md
@.planning/phases/06-advanced-agents/06-01-SUMMARY.md
@.planning/phases/06-advanced-agents/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from prior plan outputs -->

From src/lib/ciso-tools.ts (after 06-02, will include delegateToIR + delegateToAppSec):
```typescript
export const CISO_TOOL_NAMES = [
  'delegateToGRC',
  'delegateToSOC',
  'delegateToThreatIntel',
  'delegateToIR',
  'delegateToAppSec',
  'readCompletedTaskResults',
  'getCurrentRiskPosture',
  'createFollowUpTask',
] as const;
```

From supabase/functions/agent-worker/index.ts (pre-registered):
```typescript
"pen-test": "agent-pen-test",
```

Authorization gate pattern from 06-RESEARCH.md:
- checkScanAuthorization queries agent_permissions for pen_test agent type
- Returns { authorized: boolean, reason?: string, scope?: string, restrictions?: string[] }
- CISO delegation sets risk_level='high' for double-gate (permission + approval)

Passive-only constraint:
- PEN_TEST_SYSTEM_PROMPT must state: "You have NO access to external networks. You can ONLY query internal database tables."
- Tool parameters must NOT accept URLs, IP addresses, or hostnames
- All data comes from onboarding_profiles.primary_tech_stack and threat_intelligence table
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pen Test schemas, tools, authorization gate, migration, and test suite</name>
  <files>
    supabase/functions/_shared/pen-test-schemas.ts,
    supabase/functions/_shared/pen-test-tools.ts,
    supabase/migrations/20260327400002_pen_test_agent_tables.sql,
    src/lib/pen-test-schemas-frontend.ts,
    src/lib/pen-test-tools-testable.ts,
    src/types/pen-test-output.ts,
    src/lib/__tests__/pen-test-schemas.test.ts,
    src/lib/__tests__/pen-test-agent.test.ts,
    src/lib/__tests__/pen-test-migration.test.ts
  </files>
  <behavior>
    - PenTestFindingSchema validates finding_type enum (known_cve, version_mismatch, eol_software, missing_patch), risk_rating enum (critical/high/medium/low), title, affected_technology, matched_cve_ids array, exploitability_score optional, business_impact, remediation, cmmc_controls array, status enum (open, remediated, accepted_risk, false_positive)
    - VulnerabilityReportSchema aggregates findings array, summary, overall_risk_rating, scan_scope (passive_only), authorization_reference, recommendations array
    - AuthorizationResultSchema validates authorized boolean, reason optional, scope enum, restrictions array
    - PEN_TEST_SYSTEM_PROMPT contains explicit PASSIVE ONLY constraint: "You have NO access to external networks. You can ONLY query internal database tables."
    - PEN_TEST_SYSTEM_PROMPT references CMMC controls for vulnerability scanning (3.11.2 scan for vulnerabilities, 3.11.3 remediate vulnerabilities)
    - Pen Test tools include checkScanAuthorization (queries agent_permissions), getCompanyTechStack (queries onboarding_profiles), matchTechStackCVEs (queries threat_intelligence by tech stack keywords), createPenTestFinding (inserts pen_test_findings)
    - NO tool accepts URL, IP address, or hostname parameters (Pitfall 2 prevention)
    - checkScanAuthorization returns authorized=false if pen-test agent is not enabled for the company
    - Migration creates pen_test_findings with finding_type CHECK (4 values), risk_rating CHECK (4 values), status CHECK (4 values), scan_authorization_id UUID, RLS, indexes
    - Node mirrors export PEN_TEST_SYSTEM_PROMPT, PEN_TEST_TOOL_NAMES matching Deno module
  </behavior>
  <action>
    Create Pen Test Zod schemas in supabase/functions/_shared/pen-test-schemas.ts: PenTestFindingSchema (with finding_type enum: known_cve, version_mismatch, eol_software, missing_patch; risk_rating enum; matched_cve_ids TEXT array; exploitability_score optional decimal; business_impact; remediation; cmmc_controls array), VulnerabilityReportSchema (findings array, summary, overall_risk_rating, scan_scope fixed to 'passive_only', authorization_reference UUID, recommendations array), AuthorizationResultSchema.

    Create Pen Test tools in supabase/functions/_shared/pen-test-tools.ts with PEN_TEST_SYSTEM_PROMPT. The system prompt MUST contain these exact constraints:
    - "You are the Pen Test agent for ASSESS-AI. You perform PASSIVE vulnerability discovery ONLY."
    - "You have NO access to external networks. You can ONLY query internal database tables."
    - "You MUST call checkScanAuthorization FIRST before any other tool. If authorization fails, stop immediately."
    - "Your analysis matches the company's declared tech stack against known CVE patterns. No active exploitation, no network scanning, no port probing."
    - Reference CMMC controls 3.11.2 (scan for vulnerabilities) and 3.11.3 (remediate vulnerabilities).

    4 tools: checkScanAuthorization (queries agent_permissions for pen_test agent type for the company; returns authorized/unauthorized with scope and restrictions), getCompanyTechStack (queries onboarding_profiles.primary_tech_stack), matchTechStackCVEs (takes tech_stack_keywords string array, queries threat_intelligence table WHERE description ILIKE any keyword OR affected_products matching; returns matching CVEs with severity and CVSS scores), createPenTestFinding (inserts pen_test_findings row).

    CRITICAL: No tool parameter accepts URLs, IP addresses, or hostnames. matchTechStackCVEs takes string keywords only (e.g., "apache", "nginx", "react") NOT URLs.

    Create SQL migration 20260327400002_pen_test_agent_tables.sql with pen_test_findings table matching 06-RESEARCH.md schema. CHECK constraints for finding_type (4 values), risk_rating (4 values), status (4 values). scan_authorization_id UUID column. RLS: authenticated SELECT own company, service_role ALL. Indexes on (company_id, status), (company_id, risk_rating), (affected_technology).

    Create Node mirrors and frontend types following established pattern.

    Write tests FIRST (RED), then implement (GREEN). Key test assertions: PEN_TEST_SYSTEM_PROMPT contains "PASSIVE" and "NO access to external networks", no tool parameter schema includes url/ip/hostname fields, checkScanAuthorization returns unauthorized when permission missing.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/pen-test-schemas.test.ts src/lib/__tests__/pen-test-agent.test.ts src/lib/__tests__/pen-test-migration.test.ts --bail 1</automated>
  </verify>
  <done>Pen Test Zod schemas validate all finding types and vulnerability report structure. PEN_TEST_SYSTEM_PROMPT enforces passive-only constraint. 4 tools with no external network access. Authorization gate checks agent_permissions. Migration creates pen_test_findings with all CHECK constraints and RLS. Node mirrors match Deno modules. All Pen Test-specific tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pen Test Edge Function and CISO delegateToPenTest extension (completes 7-agent team)</name>
  <files>
    supabase/functions/agent-pen-test/index.ts,
    supabase/functions/_shared/ciso-tools.ts,
    src/lib/ciso-tools.ts,
    src/lib/__tests__/ciso-agent.test.ts
  </files>
  <behavior>
    - Pen Test Edge Function handles 3 actions: passive-scan, tech-stack-cve-match, generate-vulnerability-report
    - Pen Test Edge Function uses maxSteps: 8 and claude-sonnet-4-20250514
    - Pen Test Edge Function calls executeAgentTask with Pen Test tools and PEN_TEST_SYSTEM_PROMPT
    - CISO delegateToPenTest tool exists and delegates to 'pen-test' agent type
    - delegateToPenTest sets risk_level: 'high' to trigger approval gate (double-gate with checkScanAuthorization)
    - CISO_TOOL_NAMES includes 'delegateToPenTest' (9 tools total: 6 delegations + 3 utility -- full 7-agent team)
    - CISO_SYSTEM_PROMPT has "Pen Test Delegation" section appended (5-8 lines)
    - CISO_SYSTEM_PROMPT retains ALL existing sections (GRC, SOC, ThreatIntel, IR, AppSec)
    - buildCisoPrompt handles 'passive-vulnerability-scan' action
    - Node mirror ciso-tools.ts matches Deno module updates
    - All existing CISO tests still pass plus new Pen Test delegation tests
    - CISO_TOOL_NAMES has exactly 9 entries (6 delegateToX + 3 utility tools)
  </behavior>
  <action>
    Create Pen Test Edge Function at supabase/functions/agent-pen-test/index.ts following the agent skeleton. maxSteps: 8, model: claude-sonnet-4-20250514. Handle 3 actions: passive-scan (calls checkScanAuthorization first, then getCompanyTechStack, then matchTechStackCVEs, creates findings), tech-stack-cve-match (focused CVE matching for specific tech stack component), generate-vulnerability-report (aggregates all findings into VulnerabilityReport).

    Extend CISO Deno tools (supabase/functions/_shared/ciso-tools.ts): Add delegateToPenTest tool that delegates to 'pen-test' agent type. CRITICAL: delegateToPenTest MUST set risk_level: 'high' to trigger the approval gate. This creates the double-gate: (1) checkScanAuthorization verifies the company has enabled pen testing, (2) risk_level='high' triggers human approval. Add CISO_SYSTEM_PROMPT section "## Pen Test Delegation" (5-8 lines max). Key rules: passive only, requires company authorization, no active exploitation. Update CISO_TOOL_NAMES to include 'delegateToPenTest'. Add buildCisoPrompt case for 'passive-vulnerability-scan'.

    APPEND-ONLY: Do NOT rewrite existing GRC, SOC, Threat Intel, IR, or AppSec delegation rules. Append Pen Test section as the final delegation section.

    Update Node mirror src/lib/ciso-tools.ts with matching additions. Final CISO_TOOL_NAMES should have 9 entries: delegateToGRC, delegateToSOC, delegateToThreatIntel, delegateToIR, delegateToAppSec, delegateToPenTest, readCompletedTaskResults, getCurrentRiskPosture, createFollowUpTask.

    Update src/lib/__tests__/ciso-agent.test.ts: Add tests for delegateToPenTest in CISO_TOOL_NAMES, Pen Test delegation prompt section present, buildCisoPrompt case, Pen Test Edge Function file structure. Add a comprehensive test: CISO_TOOL_NAMES length is 9 (confirming all 6 specialist delegations + 3 utility tools = complete 7-agent team).

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/ciso-agent.test.ts src/lib/__tests__/pen-test-agent.test.ts --bail 1</automated>
  </verify>
  <done>Pen Test Edge Function operational with 3 actions and passive-only constraint. CISO Orchestrator can delegate to all 6 specialist agents (GRC, SOC, ThreatIntel, IR, AppSec, PenTest). delegateToPenTest sets risk_level='high' for double-gate authorization. CISO_TOOL_NAMES has 9 entries (complete team). All existing CISO tests still pass. Node mirrors match Deno modules. Full test suite green. 7-agent security team complete.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vitest run` -- full suite must pass (all prior tests + IR tests + AppSec tests + new Pen Test tests)
2. Pen Test Edge Function file exists at supabase/functions/agent-pen-test/index.ts
3. pen_test_findings migration has CREATE TABLE with all CHECK constraints
4. CISO_TOOL_NAMES has exactly 9 entries in both Deno and Node modules
5. CISO_SYSTEM_PROMPT retains ALL 6 delegation sections (GRC, SOC, ThreatIntel, IR, AppSec, PenTest)
6. PEN_TEST_SYSTEM_PROMPT contains "PASSIVE" and "NO access to external networks"
7. No Pen Test tool parameter accepts URL, IP, or hostname
8. delegateToPenTest sets risk_level='high'
9. All 7 agent types have corresponding Edge Functions and CISO delegation tools
</verification>

<success_criteria>
- Pen Test agent is PASSIVE ONLY -- no external network access, no URL/IP/hostname parameters in tools
- Pen Test agent matches declared tech stack against threat_intelligence CVE data
- Vulnerability reports include risk ratings, exploitability assessment, and business impact
- Double authorization gate: agent_permissions check + risk_level='high' approval
- pen_test_findings table created with finding types and lifecycle status
- CISO delegates to all 6 specialist agents (complete hub-and-spoke topology)
- CISO_TOOL_NAMES has 9 entries (6 delegations + 3 utility)
- Full 7-agent security team operational
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-agents/06-03-SUMMARY.md`
</output>
