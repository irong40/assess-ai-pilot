---
phase: 06-advanced-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/agent-incident-response/index.ts
  - supabase/functions/_shared/ir-schemas.ts
  - supabase/functions/_shared/ir-tools.ts
  - supabase/functions/_shared/ciso-tools.ts
  - supabase/migrations/20260327400000_ir_agent_tables.sql
  - src/lib/ir-schemas-frontend.ts
  - src/lib/ir-tools-testable.ts
  - src/lib/ciso-tools.ts
  - src/types/ir-output.ts
  - src/lib/__tests__/ir-schemas.test.ts
  - src/lib/__tests__/ir-agent.test.ts
  - src/lib/__tests__/ir-migration.test.ts
  - src/lib/__tests__/ciso-agent.test.ts
autonomous: true
requirements: [IR-01, IR-02, IR-03, IR-04]

must_haves:
  truths:
    - "IR agent generates containment recommendations with incident type classification and NIST 800-61r2 four-phase steps"
    - "IR agent provides step-by-step playbook guidance with ordered detect/contain/eradicate/recover phases"
    - "IR agent generates post-incident reports with timeline, root cause, lessons learned, and compliance impact"
    - "ALL IR tasks are risk_level='high' -- every recommendation requires human approval before any action"
    - "CISO Orchestrator can delegate to IR agent via delegateToIR tool"
  artifacts:
    - path: "supabase/functions/agent-incident-response/index.ts"
      provides: "IR Edge Function handler with 3 actions"
      contains: "executeAgentTask"
    - path: "supabase/functions/_shared/ir-schemas.ts"
      provides: "Zod schemas for IR structured output"
      exports: ["IncidentTypeSchema", "ContainmentRecommendationSchema", "PostIncidentReportSchema"]
    - path: "supabase/functions/_shared/ir-tools.ts"
      provides: "IR system prompt and 5 domain tools"
      exports: ["IR_SYSTEM_PROMPT", "createIrTools"]
    - path: "supabase/migrations/20260327400000_ir_agent_tables.sql"
      provides: "ir_incidents table with lifecycle status and RLS"
      contains: "CREATE TABLE ir_incidents"
    - path: "src/lib/__tests__/ir-schemas.test.ts"
      provides: "Schema validation tests for IR output"
      min_lines: 30
    - path: "src/lib/__tests__/ir-agent.test.ts"
      provides: "Agent handler and tool tests for IR"
      min_lines: 30
  key_links:
    - from: "supabase/functions/_shared/ciso-tools.ts"
      to: "supabase/functions/agent-incident-response/index.ts"
      via: "delegateToIR tool delegates tasks to incident-response agent type"
      pattern: "delegateToIR"
    - from: "supabase/functions/_shared/ir-tools.ts"
      to: "soc_alerts"
      via: "getEscalatedIncidents queries escalation_status='needs_ir_review'"
      pattern: "needs_ir_review"
    - from: "src/lib/ir-tools-testable.ts"
      to: "src/lib/__tests__/ir-agent.test.ts"
      via: "Node mirror tested by vitest"
      pattern: "IR_SYSTEM_PROMPT"
---

<objective>
Incident Response agent with NIST 800-61r2 containment playbooks, post-incident reports, and mandatory human approval on all actions.

Purpose: The IR agent consumes SOC-escalated incidents (escalation_status='needs_ir_review') and produces containment recommendations, step-by-step playbooks, and post-incident reports that feed into the compliance dashboard. ALL IR tasks are hardcoded to risk_level='high' to guarantee human approval before any action.

Output: IR Edge Function, Zod schemas, 5 domain tools, ir_incidents migration, CISO delegateToIR extension, vitest-compatible Node mirrors, test suite.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-advanced-agents/06-RESEARCH.md
@.planning/phases/05-security-operations-agents/05-01-SUMMARY.md
@.planning/phases/05-security-operations-agents/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/agent.ts:
```typescript
export type AgentType =
  | 'ciso-orchestrator'
  | 'grc-analyst'
  | 'soc-analyst'
  | 'threat-intel'
  | 'incident-response'
  | 'appsec'
  | 'pen-test';
```

From src/lib/ciso-tools.ts (current CISO_TOOL_NAMES):
```typescript
export const CISO_TOOL_NAMES = [
  'delegateToGRC',
  'delegateToSOC',
  'delegateToThreatIntel',
  'readCompletedTaskResults',
  'getCurrentRiskPosture',
  'createFollowUpTask',
] as const;
```

From supabase/functions/agent-worker/index.ts (pre-registered):
```typescript
"incident-response": "agent-incident-response",
```

Established agent skeleton (from SOC/Threat Intel):
- Deno Edge Function with executeAgentTask + generateText
- maxSteps: 8, model: claude-sonnet-4-20250514
- Deno schemas in _shared/xxx-schemas.ts
- Deno tools in _shared/xxx-tools.ts with createXxxTools factory
- Node mirrors: src/lib/xxx-schemas-frontend.ts, src/lib/xxx-tools-testable.ts
- Frontend types: src/types/xxx-output.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IR schemas, tools, migration, and test suite</name>
  <files>
    supabase/functions/_shared/ir-schemas.ts,
    supabase/functions/_shared/ir-tools.ts,
    supabase/migrations/20260327400000_ir_agent_tables.sql,
    src/lib/ir-schemas-frontend.ts,
    src/lib/ir-tools-testable.ts,
    src/types/ir-output.ts,
    src/lib/__tests__/ir-schemas.test.ts,
    src/lib/__tests__/ir-agent.test.ts,
    src/lib/__tests__/ir-migration.test.ts
  </files>
  <behavior>
    - IncidentTypeSchema validates all 9 types (malware, unauthorized_access, denial_of_service, data_breach, insider_threat, supply_chain, misconfiguration, policy_violation, unknown) and rejects invalid types
    - ContainmentRecommendationSchema requires incident_type, severity enum, containment_strategy enum, steps array with phase/order/action/rationale/requires_approval=true, cmmc_controls_affected array, compliance_impact string
    - PostIncidentReportSchema requires incident_id UUID, timeline array, detection_method, containment/eradication/recovery actions arrays, root_cause_analysis, lessons_learned array, compliance_impact object with affected_controls/sprs_impact/requires_poam_update, recommendations array
    - IR_SYSTEM_PROMPT contains NIST 800-61r2 four-phase lifecycle keywords (detect, contain, eradicate, recover), mandatory approval rule, CMMC controls 3.6.1/3.6.2/3.6.3 references
    - IR tools include getEscalatedIncidents, getIncidentContext, createIrIncident, saveContainmentPlan, savePostIncidentReport
    - IR_SYSTEM_PROMPT explicitly states ALL actions require human approval
    - Migration creates ir_incidents table with all required columns (incident_type CHECK, severity CHECK, status lifecycle CHECK with 6 states, containment_plan JSONB, playbook JSONB, post_incident_report JSONB, compliance_impact JSONB), RLS policies, indexes on (company_id, status), (company_id, created_at DESC), (soc_alert_id)
    - Node mirror (ir-tools-testable.ts) exports IR_SYSTEM_PROMPT, IR_TOOL_NAMES matching Deno module
    - Node mirror (ir-schemas-frontend.ts) exports all Zod schemas matching Deno module
  </behavior>
  <action>
    Create IR Zod schemas in supabase/functions/_shared/ir-schemas.ts following the exact patterns from 06-RESEARCH.md code examples: IncidentTypeSchema (9 enum values), ContainmentRecommendationSchema (with steps array where requires_approval is z.literal(true)), PlaybookGuidanceSchema, PostIncidentReportSchema (with compliance_impact object), IrAnalysisResultSchema (aggregating all output types).

    Create IR tools in supabase/functions/_shared/ir-tools.ts with IR_SYSTEM_PROMPT encoding NIST SP 800-61r2 four-phase lifecycle (detect, contain, eradicate, recover). System prompt MUST state: "ALL your recommendations require human approval. Every task you process has risk_level='high'." Reference CMMC controls 3.6.1 (IR plan), 3.6.2 (IR testing), 3.6.3 (IR reporting). Include 5 tools: getEscalatedIncidents (queries soc_alerts with escalation_status='needs_ir_review'), getIncidentContext (fetches threat_briefs, ioc_tracking, soc_alert_correlations), createIrIncident (inserts ir_incidents row), saveContainmentPlan (updates ir_incidents.containment_plan JSONB), savePostIncidentReport (updates ir_incidents.post_incident_report JSONB AND inserts compliance_snapshot row linking incident to compliance impact per Pitfall 6).

    Create SQL migration 20260327400000_ir_agent_tables.sql with ir_incidents table matching the schema from 06-RESEARCH.md Database Schema Design section. Include CHECK constraints for incident_type (9 values), severity (4 values), status (6 lifecycle states). Add RLS: authenticated users SELECT own company, service_role ALL. Add indexes on (company_id, status), (company_id, created_at DESC), (soc_alert_id).

    Create Node mirrors: src/lib/ir-schemas-frontend.ts re-exports all Zod schemas using npm zod (not Deno). src/lib/ir-tools-testable.ts re-exports IR_SYSTEM_PROMPT, IR_TOOL_NAMES, buildIrPrompt using same string values.

    Create frontend types: src/types/ir-output.ts with TypeScript interfaces derived from Zod schemas (IrIncident, ContainmentRecommendation, PostIncidentReport, PlaybookGuidance).

    Write tests FIRST (RED phase), then implement (GREEN phase). Test files: ir-schemas.test.ts (schema validation), ir-agent.test.ts (tool names, prompt content, prompt builder), ir-migration.test.ts (table structure, RLS, indexes, CHECK constraints).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/ir-schemas.test.ts src/lib/__tests__/ir-agent.test.ts src/lib/__tests__/ir-migration.test.ts --bail 1</automated>
  </verify>
  <done>IR Zod schemas validate all incident types and containment/playbook/report structures. IR tools include 5 domain-specific tools with NIST 800-61r2 system prompt. Migration creates ir_incidents with lifecycle status, RLS, and indexes. Node mirrors match Deno modules. All IR-specific tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IR Edge Function and CISO delegateToIR extension</name>
  <files>
    supabase/functions/agent-incident-response/index.ts,
    supabase/functions/_shared/ciso-tools.ts,
    src/lib/ciso-tools.ts,
    src/lib/__tests__/ciso-agent.test.ts
  </files>
  <behavior>
    - IR Edge Function handles 3 actions: analyze-incident, generate-playbook, create-post-incident-report
    - IR Edge Function uses maxSteps: 8 and claude-sonnet-4-20250514
    - IR Edge Function calls executeAgentTask with IR tools and IR_SYSTEM_PROMPT
    - CISO delegateToIR tool exists and delegates to 'incident-response' agent type
    - delegateToIR ALWAYS sets risk_level: 'high' regardless of action type (IR-04 hard constraint)
    - CISO_TOOL_NAMES includes 'delegateToIR' (7 tools total: 3 existing delegations + IR + 3 utility)
    - CISO_SYSTEM_PROMPT has "Incident Response Delegation" section appended (5-8 lines, not rewriting existing GRC/SOC/ThreatIntel sections)
    - buildCisoPrompt handles 'handle-incident' and 'post-incident-review' actions
    - Node mirror ciso-tools.ts matches Deno module's CISO_TOOL_NAMES and CISO_SYSTEM_PROMPT additions
    - Existing CISO tests still pass plus new IR delegation tests
  </behavior>
  <action>
    Create IR Edge Function at supabase/functions/agent-incident-response/index.ts following the exact agent skeleton from SOC/Threat Intel (Deno.serve, CORS preflight, service role client, task fetch, executeAgentTask wrapper). Use maxSteps: 8, model: claude-sonnet-4-20250514. Handle 3 actions: analyze-incident (queries escalated SOC alerts, creates IR incident, generates containment plan), generate-playbook (generates step-by-step playbook for existing incident), create-post-incident-report (generates post-incident report and updates compliance snapshot).

    Extend CISO Deno tools (supabase/functions/_shared/ciso-tools.ts): Add delegateToIR tool that delegates to 'incident-response' agent type. CRITICAL: In the delegateToIR execute function, ALWAYS set risk_level: 'high' in the delegateTask() call regardless of action. This is the mechanism that guarantees IR-04 (human approval required). Add CISO_SYSTEM_PROMPT section "## Incident Response Delegation" (5-8 lines max, concise bullets per Pitfall 3). Update CISO_TOOL_NAMES to include 'delegateToIR'. Add buildCisoPrompt cases for 'handle-incident' and 'post-incident-review'.

    APPEND-ONLY: Do NOT rewrite existing GRC, SOC, or Threat Intel delegation rules in CISO_SYSTEM_PROMPT. Append the new IR section after the Threat Intelligence section.

    Update Node mirror src/lib/ciso-tools.ts with matching CISO_TOOL_NAMES array (add 'delegateToIR'), matching CISO_SYSTEM_PROMPT IR delegation section, and matching buildCisoPrompt cases.

    Update src/lib/__tests__/ciso-agent.test.ts: Add tests validating delegateToIR exists in CISO_TOOL_NAMES, IR delegation prompt section present, buildCisoPrompt returns correct strings for 'handle-incident' and 'post-incident-review', and IR Edge Function file exists with correct structure.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/ciso-agent.test.ts src/lib/__tests__/ir-agent.test.ts --bail 1</automated>
  </verify>
  <done>IR Edge Function operational with 3 actions. CISO Orchestrator can delegate to IR via delegateToIR with risk_level='high' hardcoded. All existing CISO tests still pass. Node mirrors match Deno modules. Full test suite green.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vitest run` -- full suite must pass (existing 391 tests + new IR tests)
2. IR Edge Function file exists at supabase/functions/agent-incident-response/index.ts
3. ir_incidents migration has CREATE TABLE with all CHECK constraints
4. CISO_TOOL_NAMES includes 'delegateToIR' in both Deno and Node modules
5. CISO_SYSTEM_PROMPT has IR delegation section but retains all existing GRC/SOC/ThreatIntel sections
6. All IR tasks use risk_level='high' (grep for risk_level in ir-tools.ts and ciso-tools.ts)
</verification>

<success_criteria>
- IR agent schemas validate containment recommendations, playbooks, and post-incident reports per NIST 800-61r2
- IR agent has 5 tools: getEscalatedIncidents, getIncidentContext, createIrIncident, saveContainmentPlan, savePostIncidentReport
- ir_incidents table created with lifecycle status (open/investigating/contained/eradicated/recovered/closed)
- ALL IR tasks hardcoded to risk_level='high' (IR-04 mandatory approval)
- Post-incident reports update compliance_snapshots (Pitfall 6 addressed)
- CISO delegates to IR after SOC escalation (hub-and-spoke maintained)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-advanced-agents/06-01-SUMMARY.md`
</output>
